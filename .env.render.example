# Variables de Entorno para Render
# Copia estos valores y agrégalos en Render Dashboard → Settings → Environment

# =====================================================
# OBLIGATORIAS
# =====================================================

# Perfil de Spring Boot (dejar en "production")
SPRING_PROFILES_ACTIVE=production

# URL de la base de datos PostgreSQL
# Obtenla de: Render Dashboard → Tu Base de Datos → Internal Database URL
DATABASE_URL=postgresql://synapsse:XXXXXXXX@oregon-postgres.render.com/synapsse

# Secreto para firmar tokens JWT (genera uno seguro)
# Usa: generate-jwt-secret.bat o generate-jwt-secret.ps1
JWT_SECRET=tu_jwt_secret_super_seguro_en_base64

# =====================================================
# OPCIONALES (con valores por defecto)
# =====================================================

# Tiempo de expiración del JWT en milisegundos (24 horas)
JWT_EXPIRATION=86400000

# Opciones de la JVM (ajusta según el plan de Render)
JAVA_OPTS=-Xmx512m -Xms256m

# Puerto (Render lo configura automáticamente, pero puedes especificarlo)
PORT=8080

# =====================================================
# NOTAS IMPORTANTES
# =====================================================

# 1. DATABASE_URL debe ser la "Internal Database URL" de Render
#    (es más rápida que la External)
#
# 2. JWT_SECRET debe ser un string largo y aleatorio en Base64
#    Genera uno con: ./generate-jwt-secret.ps1
#
# 3. NO incluyas este archivo en Git
#
# 4. Después de configurar las variables, Render redesplegará
#    automáticamente tu aplicación
